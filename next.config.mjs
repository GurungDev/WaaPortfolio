/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['en', 'de'], // List your supported locales here
      defaultLocale: 'en', // Default locale
    },
  };
  
  export default nextConfig;